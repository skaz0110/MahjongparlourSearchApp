<h1>トップページ</h1>

  
    
    <% if user_signed_in? %>
    <p>ログインしています</p>
    <% else %>
      <p>ログインしていません</p>
    <% end %>
    

<%= link_to 'ゲストログイン（閲覧用）', homes_guest_sign_in_path, method: :post %>