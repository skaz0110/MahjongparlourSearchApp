コメント編集画面

<%=@review.content%>

<%= form_with url:post_review_path do |f| %>
      <%= f.text_area @review.content %>
      <%
=begin%>
 <%= f.hidden_field :post_id, value: @post.id %> 
<%
=end%>
                 
      <%= f.submit  'コメントする'%>
<%end%>   

